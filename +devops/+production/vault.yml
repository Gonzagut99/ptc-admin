# No secrets needed for frontend — environment variables are passed as Docker build args.
# If future secrets are needed, add them here and encrypt with:
# ansible-vault encrypt +devops/+production/vault.yml
